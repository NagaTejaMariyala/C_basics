/* Header file for array functions */
// By M Naga Teja on 11-07-2018

#include<stdio.h>
#include<math.h>
#include<stdlib.h>
 
double AM (double [], long int );

double GM (double [], long int );

double HM (double [], long int );

double R (double [], long int );

double D (double [], long int );

double M (double [], long int );

void BS (long int [], long int );

void Sw (long int *,long int *);

void SS (long int [], long int );

void Mg (long int [],long int);

void MS (long int [],long int);

long int Ls (long int [], long int, long int);

long int Bs (long int [], long int, long int, long int);

long int RBs (long int [], long int, long int, long int);
