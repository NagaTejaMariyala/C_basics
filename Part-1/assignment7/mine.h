/* Header file for myfunctions */
// By M Naga Teja On 04-07-2018

#include<stdio.h>
#include<math.h>

long int fact (long int );

long int C (long int , long int );

long int f10t2 (long int );

double exp (double);

double sine (double);

double cosine (double);

void add (float ,float , float  ,float );

void sub (float  ,float , float  ,float );

void pro (float  ,float , float ,float );

void div (float ,float , float  ,float );

double avg (int );

double pi1 (long int );

double pi2 (long int );

double pi3 (long int );

void swap (long int ,long int );

double r (double, double, double);

double t (double, double, double);

double f (double, double, double);

long int gcd (long int , long int);

long int S (long int);

long int F (long int );

long int FS (long int);

long int S1 (long int );

 
